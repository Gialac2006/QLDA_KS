# Tên ?ng d?ng
spring.application.name=NHOM_QTDA

# K?t n?i database MySQL (localhost, db tên 'dangky')
spring.datasource.url=jdbc:mysql://localhost:3307/dangky
spring.datasource.username=root
spring.datasource.password=password
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate/JPA t? t?o/t? c?p nh?t b?ng khi ch?y app
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.generate-ddl=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# C?u hình Thymeleaf (ch?y file trong /templates/)
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.cache=false

# Port server (n?u dùng c?ng khác s?a ? ?ây)
server.port=8080

# C?u hình CORS cho API (cho phép m?i origin/method/header)
spring.mvc.cors.allowed-origins=*
spring.mvc.cors.allowed-methods=GET,POST,PUT,DELETE
spring.mvc.cors.allowed-headers=*
